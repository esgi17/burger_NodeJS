const ModelIndex = require('../models');
const Login = ModelIndex.User;
const Op = ModelIndex.sequelize.Op;

const LoginController = function() { };

// Export du controller
module.exports = LoginController;
